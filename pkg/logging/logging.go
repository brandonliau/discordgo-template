package logging

// discord.LogLevel = discordgo.LogDebug

// bot.AddHandler(func(s *discordgo.Session, event *discordgo.Connect) {
//     log.Println("Bot connected successfully.")
// })

// bot.AddHandler(func(s *discordgo.Session, event *discordgo.Disconnect) {
//     log.Println("Bot disconnected.")
// })

// bot.AddHandler(func(s *discordgo.Session, event *discordgo.Ready) {
//     log.Println("Bot is ready.")
// })

// bot.AddHandler(func(s *discordgo.Session, event *discordgo.Resumed) {
//     log.Println("Bot resumed connection.")
// })

// bot.AddHandler(func(s *discordgo.Session, err *discordgo.EventError) {
//     log.Printf("Error received from Discord: %v", err)
// })

// bot.AddHandler(func(s *discordgo.Session, event *discordgo.HeartbeatAck) {
//     log.Println("Heartbeat acknowledged.")
// })

// bot.AddHandler(func(s *discordgo.Session, event *discordgo.HeartbeatSent) {
//     log.Println("Heartbeat sent.")
// })
